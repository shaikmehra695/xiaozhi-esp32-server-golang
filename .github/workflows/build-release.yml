# 基于 opus 环境的 Go 多平台编译 + Release
# 含 frontend（embed_ui）、manager/backend、asr_server（子模块）；需 opus + ten_vad + sherpa-onnx 运行时
name: Build Go binaries

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  workflow_dispatch:

env:
  OPUS_VERSION: "1.3.1"

permissions:
  contents: write

jobs:
  build-windows-amd64:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify asr_server submodule
        run: |
          if [ ! -f asr_server/server/setup.go ]; then
            echo "::error::asr_server 子模块未拉取，binary 构建需要 voice_server 包"
            ls -la asr_server 2>/dev/null || true
            exit 1
          fi

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.4"

      - name: Set up Node (frontend)
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: manager/frontend/package-lock.json

      - name: Build frontend (embed_ui)
        run: |
          cd manager/frontend && npm ci && npm run build
          mkdir -p ../backend/static/dist
          cp -r dist/* ../backend/static/dist/

      - name: Install cross-compilation tools & build libopus (static)
        run: |
          sudo apt-get update
          sudo apt-get install -y mingw-w64 pkg-config build-essential wget upx-ucl
          wget -q "https://archive.mozilla.org/pub/opus/opus-${OPUS_VERSION}.tar.gz"
          tar xf "opus-${OPUS_VERSION}.tar.gz"
          pushd "opus-${OPUS_VERSION}"
          ./configure \
            --host=x86_64-w64-mingw32 \
            --disable-shared --enable-static \
            --disable-doc --disable-extra-programs \
            --prefix="$HOME/opus-win"
          make -j"$(nproc)"
          make install
          popd

      - name: Configure env for Windows build
        run: |
          echo "PKG_CONFIG_PATH=$HOME/opus-win/lib/pkgconfig" >> $GITHUB_ENV
          echo "CGO_ENABLED=1" >> $GITHUB_ENV
          echo "GOOS=windows" >> $GITHUB_ENV
          echo "GOARCH=amd64" >> $GITHUB_ENV
          echo "CC=x86_64-w64-mingw32-gcc" >> $GITHUB_ENV

      - name: Build Windows binary
        run: |
          go mod tidy
          # ten_vad 为动态库，不加 -static
          go build -v -tags "nolibopusfile asr_server manager embed_ui" \
            -ldflags "-s -w" \
            -o xiaozhi_server.exe ./cmd/server

      - name: Compress with UPX & keep single exe
        run: |
          upx --lzma xiaozhi_server.exe || true

      - name: Pack Windows release（目录 windows-amd64/，artifact 传目录不传 zip，避免下载后再解压一层 zip）
        run: |
          PACK_DIR="windows-amd64/xiaozhi-server-windows-amd64"
          mkdir -p "$PACK_DIR"/data "$PACK_DIR"/logs

          cp xiaozhi_server.exe "$PACK_DIR/"
          cp build/common/main_config.yaml build/common/manager.json build/common/asr_server.json "$PACK_DIR/"
          cp -r build/common/models "$PACK_DIR/"
          cp -r build/common/data/* "$PACK_DIR/data/"
          cp build/windows/start.bat build/windows/README.md "$PACK_DIR/"
          cp build/windows/onnxruntime.dll build/windows/sherpa-onnx-c-api.dll build/windows/sherpa-onnx-cxx-api.dll build/windows/ten_vad.dll "$PACK_DIR/"

      - name: Upload Windows package（传目录，下载解压后为 windows-amd64/ 一层）
        uses: actions/upload-artifact@v4
        with:
          name: xiaozhi-server-windows-amd64
          path: windows-amd64

  build-linux-amd64:
    # 使用 Ubuntu 22.04 构建，使二进制依赖的 glibc 版本较低，便于在旧系统上运行
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify asr_server submodule
        run: |
          if [ ! -f asr_server/server/setup.go ]; then
            echo "::error::asr_server 子模块未拉取，binary 构建需要 voice_server 包"
            ls -la asr_server 2>/dev/null || true
            exit 1
          fi

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.4"

      - name: Set up Node (frontend)
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: manager/frontend/package-lock.json

      - name: Build frontend (embed_ui)
        run: |
          cd manager/frontend && npm ci && npm run build
          mkdir -p ../backend/static/dist
          cp -r dist/* ../backend/static/dist/

      - name: Install deps & build libopus (static)
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config build-essential wget libc++-dev libc++abi-dev upx-ucl zip patchelf
          wget -q "https://archive.mozilla.org/pub/opus/opus-${OPUS_VERSION}.tar.gz"
          tar xf "opus-${OPUS_VERSION}.tar.gz"
          pushd "opus-${OPUS_VERSION}"
          ./configure --enable-static --disable-shared --disable-doc --disable-extra-programs --prefix="$HOME/opus-linux"
          make -j"$(nproc)"
          make install
          popd

      - name: Configure env for Linux build
        run: |
          echo "PKG_CONFIG_PATH=$HOME/opus-linux/lib/pkgconfig" >> $GITHUB_ENV
          echo "CGO_ENABLED=1" >> $GITHUB_ENV
          echo "CGO_LDFLAGS=-lm" >> $GITHUB_ENV
          echo "GOOS=linux" >> $GITHUB_ENV
          echo "GOARCH=amd64" >> $GITHUB_ENV

      - name: Build Linux binary
        run: |
          go mod tidy
          # ten_vad 为 .so 动态库，不加 -static；rpath 已在 cgo 中指定；含 asr_server 与 Windows 一致
          go build -v -tags "nolibopusfile asr_server manager embed_ui" \
            -ldflags "-s -w" \
            -o xiaozhi_server ./cmd/server

      - name: UPX 压缩（仅保留此二进制，文件名不带 upx）
        run: |
          upx --lzma xiaozhi_server -o xiaozhi_server.upx && mv xiaozhi_server.upx xiaozhi_server || true

      - name: Pack Linux release（目录 linux-amd64/，解压/下载后一层目录）
        run: |
          PACK_DIR="linux-amd64/xiaozhi_server-linux-amd64"
          mkdir -p "$PACK_DIR"/ten-vad/lib/Linux/x64 "$PACK_DIR"/logs
          cp xiaozhi_server "$PACK_DIR/"
          patchelf --add-rpath '$ORIGIN/ten-vad/lib/Linux/x64' "$PACK_DIR/xiaozhi_server" 2>/dev/null || true
          cp build/linux/libten_vad.so build/linux/libsherpa-onnx-c-api.so build/linux/libonnxruntime.so "$PACK_DIR/ten-vad/lib/Linux/x64/"
          cp build/common/main_config.yaml build/common/manager.json build/common/asr_server.json "$PACK_DIR/"
          cp -r build/common/models build/common/data "$PACK_DIR/"
          chmod +x "$PACK_DIR/xiaozhi_server"

      - name: Upload Linux package（传目录 linux-amd64，下载解压后一层 linux-amd64/）
        uses: actions/upload-artifact@v4
        with:
          name: linux-amd64
          path: linux-amd64

  release:
    needs:
      - build-windows-amd64
      - build-linux-amd64
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - name: Download Windows package
        uses: actions/download-artifact@v4
        with:
          name: windows-amd64-binaries
          path: release

      - name: Download Linux package
        uses: actions/download-artifact@v4
        with:
          name: linux-amd64
          path: release

      - name: 打 Release 用 zip（解压后各一层目录 windows-amd64/、linux-amd64/）
        run: |
          cd release && zip -r windows-amd64.zip windows-amd64 && zip -r linux-amd64.zip linux-amd64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          draft: false
          prerelease: false
          files: |
            release/windows-amd64.zip
            release/linux-amd64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
